@model Validate.Result;
@{
    ViewBag.Title = "Validate - Swedish Personal Identity Numbers";
}

<h1 class="h2">
    Validate
    <small class="text-muted">Swedish Personal Identity Numbers</small>
</h1>

<p class="lead py-3">
    Pellentesque sollicitudin ex a orci hendrerit ullamcorper in in nulla. Nunc feugiat enim eget hendrerit faucibus. Nullam sed justo eros. Curabitur pellentesque ullamcorper ligula, non viverra sem blandit vel. Cras rhoncus, mi ac fringilla placerat.
</p>


<form asp-action="Validate" method="get">
    <div class="form-row">
        <div class="form-group col-md-6">
            <label asp-for="NumberInput">Swedish Personal Identity Number</label>
            <input type="tel" class="form-control" name="number" asp-for="NumberInput" required placeholder="YYMMDD-XXXX" />
        </div>
        <div class="form-group col-md-6 ii-filter-submit-group">
            <button type="submit" class="btn btn-primary btn-block">Validate</button>
        </div>
    </div>
</form>

@if (!string.IsNullOrEmpty(Model.NumberInput))
{
    <div>
        IsValid: @Model.IsValid
        @if (Model.IsValid && Model.Number.HasValue)
        {
            <span>Value: @Model.Number.Value.TwelveDigitString</span>
        }
    </div>
}